<div class="empty-state empty-state--only-child">
   <p class="empty-state__text">
    You don't have any posts yet!
  </p>

  <%= link_to "Add post",
      trips_path,
      class: "btn btn--primary",
      data: { turbo_frame: dom_id(TripPost.new) } %>
</div>