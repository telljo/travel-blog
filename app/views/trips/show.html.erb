<%= render @trip %>

<h2>Posts<h2>

<% if current_user == @trip.user %>
  <div class="container">
    <%= render partial: 'posts/form', locals: { trip: @trip, post: @post } %>
  </div>
<% end %>

<%= render @trip.posts %>

<div>
  <%= link_to "Edit this trip", edit_trip_path(@trip) %> |
  <%= link_to "Back to trips", trips_path %>
  <%= button_to "Destroy this trip", @trip, method: :delete %>
</div>
